export const metadata = { title: "איך רשת 'לומדת': Loss, Backprop, Gradient Descent | אקדמיית AI" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
